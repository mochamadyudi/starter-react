version: '3.8'
services:
  stg:
    env_file:
      - .env
    environment:
      - NODE_ENV=development
    image: app:${TAG}
    container_name: app-starter
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
